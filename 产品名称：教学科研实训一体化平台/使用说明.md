# 教学科研实训一体化平台 · Demo 工程使用说明

> 本文档面向需要接手、阅读或继续扩展本 Demo 的同事，帮你在 10 分钟内搞清楚"这个工程是什么、为什么这么设计、怎么用"。

---

## 1. 工程是什么

一个为了**演示产品理念**而制作的前端 Demo 资产集，**不含任何可运行服务**。它由两件东西组成：

1. **一份页面设计文档** — 讲清楚产品每一页长什么样、每个区域是干嘛的、数据从哪来。
2. **一套 TypeScript 假数据** — 围绕一条主故事线精心编排，覆盖 20 个页面所需的全部展示内容。

页面开发只需 `import { ... } from './mock-data'`，就能拿到完整的、前后关联一致的演示数据，免去现编数据的痛苦。

---

## 2. 目录结构

```
产品名称：教学科研实训一体化平台/
├── 产品名称：教学科研实训一体化平台.md   （原始产品文档，需求出处）
├── 页面设计文档.md                     ★ 主交付物 1：每一页的结构 + 动线
├── 使用说明.md                         （本文件）
└── mock-data/                          ★ 主交付物 2：假数据（TS 模块）
    ├── index.ts                        统一导出入口
    ├── types.ts                        全部实体类型定义（真源）
    │
    ├── professions.ts                  专业 1 个（机械工程）
    ├── subjects.ts                     学科 9 个（均机械类）
    ├── classes.ts                      班级 6 个（大一～大四）
    ├── students.ts                     学生 ~131 人
    ├── teachers.ts                     教师 6 位
    │
    ├── classProfiles.ts                班级画像 6 份
    ├── studentProfiles.ts              学生画像 10 份
    │
    ├── knowledgeGraph.ts               图谱：机械工程 72 节点
    ├── courses.ts                      课程 8 门（制图主线 + 多类型辅课）
    ├── resources.ts                    资源 45 条（8 种类型）
    ├── trainings.ts                    实训 15 个
    │
    ├── personas.ts                     AI 人设 8 个 + Skill/MCP 11 个
    ├── strategies.ts                   教学策略模板 6 个
    ├── teachingPlans.ts                教学计划 4 个
    ├── teachingDesigns.ts              教学设计（主线焦点小节 3 Tab 完整产物）
    ├── homeworks.ts                    作业评价 10 次（导出时附逐学生结果）
    ├── exams.ts                        考试评价 4 次（同上）
    └── evalResultBuilders.ts           为作业/考试行生成 studentResults
```

---

## 3. 主故事线（必须先搞清楚！）

所有数据都围绕这条故事线精心编排。**演示 / 联调时请沿这条线走，数据最连贯。**

> **李建国老师**（机械工程学院 · 制图教研室主任）在 2026 春为 **机制 2301 + 2302** 两个班同开《机械制图与CAD》。第 3 章《正投影法与三视图》第 2 节《组合体三视图绘制》是整个 Demo 的**焦点小节**。
>
> 他先在【学情分析】看到 **2302 班两极分化（σ=15.2）** → 在【教学计划】里定制策略 → 在【教学设计工作台】AI 生成讲义 + 课堂 PPT + 作业三件套 → 课后在【协同评价】看到 **截交线/相贯线 43% 集中错题** → AI 建议把下一小节扩为 3 课时 → 在【知识图谱】点到 `kn-mech-028 截交线` 节点 → 挂载资源回流到教学设计。闭环完成。

**辅线数据（同专业、多年级）**：用于列表与闭环演示，不改变主线叙事。

| 年级/班级 | 示例课程 | 教师 | 说明 |
|---|---|---|---|
| 大一 机制2401 | 《机械工程专业导论》 | 孙小艳 | 认知与课程地图 |
| 大二 机制2303 | 《工业机器人技术应用基础》 | 赵文静 | 草稿计划 + 仿真辅线 |
| 大四 机制2101 | 《互换性与技术测量》 | 王丽华 | 公差—检测—工艺衔接 |

**典型演示学生**：
- 张伟（机制 2301，空间想象力强，尖子生）
- 刘静雯（机制 2301，绘图规范典范）
- 陈浩宇（机制 2302，薄弱生 + 低活跃，用于展示关注态）
- 林诗涵（机制 2302，尖子生，佐证两极分化）

---

## 4. 假数据怎么用

### 4.1 直接引用

```ts
import {
  professions,
  courses,
  teachingPlans,
  homeworkEvaluations,
  graphNodes,
  nodesByProfession,
  // ...任何 index.ts 里 re-export 的变量
} from './mock-data';
```

**类型**也都从 `./mock-data` 一并导出：

```ts
import type {
  Course,
  TeachingPlan,
  GraphNode,
  ClassProfile,
} from './mock-data';
```

### 4.2 典型查找模式

| 场景 | 代码示例 |
|---|---|
| 查某个专业的图谱节点 | `nodesByProfession['prof-mech']` |
| 查某个图谱节点挂了哪些资源 | `resources.filter(r => r.knowledgeNodeIds.includes('kn-mech-031'))` |
| 查某门课用到哪些知识点 | `courses.find(c => c.id === 'course-mech-draw')!.knowledgeNodeIds` |
| 查某个小节的教学设计 | `designsBySection['plan-main::sec-3-2']`（已预建索引） |
| 查某个作业 / 考试评价 | `homeworkEvaluations.find(h => h.id === 'hw-m-003')` |
| 按 ID 拿图谱节点 | `nodeById['kn-mech-031']`（已预建索引） |

### 4.3 ID 命名规范（方便一眼认出所属）

| 前缀 | 含义 | 示例 |
|---|---|---|
| `prof-*` | 专业 | `prof-mech` |
| `subj-*` | 学科 | `subj-mech-drawing` |
| `cls-*` | 班级 | `cls-mech-2301` / `cls-mech-2302` |
| `s-*` | 学生 | `s-mech2301-01`（张伟） |
| `t-*` | 教师 | `t-li`（李建国） |
| `kn-<prof>-NNN` | 图谱知识点 | `kn-mech-031`（组合体三视图绘制） |
| `sk-<prof>-NNN` | 图谱技能点 | `sk-mech-003`（SolidWorks 建模） |
| `core-<prof>-NNN` | 图谱核心素养 | `core-mech-002`（空间想象） |
| `course-*` | 课程 | `course-mech-draw` |
| `res-*` | 资源 | `res-m-042`（讲义 v3） |
| `train-*` | 实训 | `train-m-003`（组合体三视图实训） |
| `plan-*` | 教学计划 | `plan-main`（主线） |
| `sec-N-M` | 计划小节 | `sec-3-2`（焦点小节） |
| `design-*` | 教学设计 | `design-sec-3-2-handout` |
| `persona-*` | AI 人设 | `persona-li-custom-1`（李建国·工程情境型） |
| `strat-*` | 教学策略 | `strat-li-personal` |
| `hw-*` | 作业评价 | `hw-m-003`（组合体作业） |
| `exam-*` | 考试评价 | `exam-m-midterm` |

### 4.4 数据关联图

```mermaid
flowchart LR
  prof[professions] --> graph[knowledgeGraph]
  prof --> subj[subjects]
  subj --> course[courses]
  graph --> course
  graph --> res[resources]
  graph --> train[trainings]
  prof --> cls[classes]
  cls --> stu[students]
  cls --> clsProf[classProfiles]
  stu --> stuProf[studentProfiles]
  course --> plan[teachingPlans]
  cls --> plan
  plan --> design[teachingDesigns]
  design --> persona[personas]
  plan --> hw[homeworks]
  plan --> exam[exams]
  hw --> clsProf
  exam --> clsProf
```

---

## 5. 页面文档怎么用

`页面设计文档.md` 是唯一的页面真源，结构固定：

```
0  全局说明                      （产品定位 · 布局 · 公共组件 · 动线图）
1  智慧教学模块（1.1 ~ 1.12）      12 个页面
2  学科知识引擎模块（2.1 ~ 2.8）   8 个页面
3  页面 × 假数据映射表            一行一页，标清用到哪些 mock 变量
4  主故事线演示动线剧本           给讲解人用
```

**每个页面的写法统一为 5 段**：

1. 页面目的
2. 进入路径
3. 布局示意（ASCII 画）
4. 关键组件/按钮
5. 状态变体（空 / 正常 / 异常）
6. 使用的假数据（会直接点名变量，如 `classProfiles` / `teachingPlans`）

**页面开发的推荐工作流**：

1. 打开 `页面设计文档.md` 找到你要做的页面（如 1.4 教学设计工作台）。
2. 看"使用的假数据"那一行，知道要 `import` 哪些变量。
3. 按 ASCII 布局 + 组件清单实现 UI。
4. 状态变体用文档里列举的场景切数据即可（如 2302 班对照态用 `hw-m-003-2302`）。

---

## 6. 数据规模速览（方便估计工作量）

| 类别 | 数量 | 备注 |
|---|---|---|
| 专业 | 1 | 机械工程 |
| 学科 | 9 | 制图 / 设计 / 制造 / CAD / 导论 / 互换性 / 机器人 / 液压 / 工艺 |
| 班级 | 6 | 2401 / 2301–2303 / 2201 / 2101 |
| 学生 | ~125 | 与各班规模或抽样一致 |
| 教师 | 6 | 均在机械工程学院 |
| 图谱节点 | 72 | 机械工程单库 |
| 图谱边 | 120+ | 先修 / 包含 / 支撑 / 相关 |
| 课程 | 8 | |
| 教学资源 | 45 | 8 种类型全覆盖 |
| 实训项目 | 15 | |
| AI 人设 | 8 | 4 预置 + 4 自定义 |
| 教学策略 | 6 | |
| 教学计划 | 5 | 主线 + 历史 + 大一导论 + 大四互换性 + 机器人草稿 |
| 教学设计 | 3 | 主线焦点小节的讲义/课堂/作业完整产物 |
| 作业评价 | 10 | 含主线 6 + 互换性 2 + 机器人 2 |
| 考试评价 | 4 | 含期末预告态 |

---

## 7. 扩展 / 修改指南

### 7.1 加一个学生

1. 去 `students.ts` 追加一条 `Student`（注意 `classId` 要存在）。
2. 可选：去 `studentProfiles.ts` 加对应画像（不加也不会报错，仅列表页少一张画像卡）。

### 7.2 加一条资源

1. 去 `resources.ts` 追加 `Resource`。
2. 关键字段：`knowledgeNodeIds`（多对多）、`courseIds`。
3. 节点 ID / 课程 ID 必须在 `graphNodes` / `courses` 里已经存在。

### 7.3 加一个图谱节点

1. 去 `knowledgeGraph.ts` 在对应 `mechNodes` / `lawNodes` / `nurseNodes` 数组末尾追加。
2. 如有关系，再在对应 `*Edges` 数组里追加 `GraphEdge`。
3. 记得按 ID 命名规范（`kn-mech-NNN` 等）。

### 7.4 加一次作业 / 考试评价

1. 在 `homeworks.ts` / `exams.ts` 追加新项（类型为 `HomeworkEvalInput` / `ExamEvalInput`：不含 `studentResults` 字段即可）。
2. 导出时由 `evalResultBuilders.ts` 的 `withHomeworkStudentResults` / `withExamStudentResults` 自动补齐 `studentResults`（与汇总字段在 Demo 级大致对齐的逐人逐题假数据）。考试若需题目行筛选，为该项填写 `questionAccuracy` 数组；未开考的考试可留空分桶与 `questionAccuracy`。
3. 必填："分数分布 + 热点错题 + 重点学生 + AI 洞察"四段式。
4. 热点错题里的 `knowledgeNodeId` 建议填实际存在的节点 ID，方便跳转资源库联动。

### 7.5 新增一个完整教学设计

主线焦点小节 `sec-3-2` 有三个设计样本（讲义 / 课堂 / 作业）可以作为模板复制：

- `design-sec-3-2-handout`
- `design-sec-3-2-class`
- `design-sec-3-2-homework`

模仿它们的 `chatHistory` 和 `outputs` 写新的即可。

### 7.6 扩展图谱或新增课程方向

1. 在 `knowledgeGraph.ts` 的 `mechNodes` / `mechEdges` 中追加节点与边，保持 `prof-mech`。
2. 在 `courses.ts` / `subjects.ts` 挂上对应课程与知识点 ID。
3. 节点数建议 ≥ 20，否则图形态过稀疏。

---

## 8. 注意事项

1. **这只是 Demo 数据**，不要把它直接对接到真实 API。真实对接时用它来做接口形状的参考，然后从 `types.ts` 抽类型。
2. **所有时间戳都以 2026 春季为基准**。想展示不同时间点时，对应地修改 `createdAt` / `plannedDate` 等字段即可。
3. **图片 / 文件 URL 都是占位字符串**（如 `/covers/course-mech-draw.jpg`）。接入时请替换为真实 CDN 或本地资源路径。
4. **教学设计的产物（`outputs`）也是展示用的元数据**，并非真的 PDF/视频文件。
5. **人设提示词、AI 洞察文案、策略模板** 都是手写的 mock 文案，真实产品里应由 LLM 实时生成。
6. **机制 2302 班刻意设计成两极分化**（σ=15.2），用于展示异常洞察能力。相关 AI 洞察文字和重点学生数据都围绕这个设定展开，改动时需要同步调整。

---

## 9. 快速索引：想看某个场景，直接找这些变量

| 想演示... | 打开/引用... |
|---|---|
| 多年级班级差异 | `classes` / `classProfiles` |
| 大一导论计划 | `teachingPlans.find(p => p.id === 'plan-mech-intro')` |
| 两极分化班级 | `classProfiles.find(p => p.classId === 'cls-mech-2302')` |
| 尖子学生画像 | `studentProfiles.find(p => p.studentId === 's-mech2301-01')` → 张伟 |
| 薄弱关注学生 | `studentProfiles.find(p => p.studentId === 's-mech2302-01')` → 陈浩宇 |
| 完整教学设计三件套 | `designsBySection['plan-main::sec-3-2']` |
| 最详细的作业评价 | `homeworkEvaluations.find(h => h.id === 'hw-m-003')` |
| 双班联考对照 | `examEvaluations.find(e => e.id === 'exam-m-midterm')` |
| 考试预告态 | `examEvaluations.find(e => e.id === 'exam-m-final')` |
| 李老师自定义人设 | `personas.filter(p => p.ownerTeacherId === 't-li')` |
| AI 生成的资源 | `resources.filter(r => r.isAiGenerated)` |
| 主线焦点实训 | `trainingProjects.find(t => t.id === 'train-m-003')` |

---

## 10. 版本信息

- **当前版本**：v1.0
- **数据基准日期**：2026 春季学期
- **页面文档**：`页面设计文档.md` · v1.0 · 2026-04
- **假数据模块**：`mock-data/` · v1.0 · 2026-04

若需扩展，请遵守第 7 节的指南；新增数据务必同步更新本说明的"数据规模速览"和"快速索引"两节。
